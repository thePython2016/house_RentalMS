<?php
/**
 * Local overrides — copy this file to config.local.php (same folder) and edit.
 *
 * Google Maps:
 * 1. Go to https://console.cloud.google.com/google/maps-apis
 * 2. Enable "Maps JavaScript API"
 * 3. Create an API key (Credentials → Create credentials → API key)
 * 4. For local XAMPP, restrict by HTTP referrer: http://localhost/*
 * 5. Paste the key below (never commit config.local.php with a real key to public repos)
 */
define('GOOGLE_MAPS_API_KEY', 'YOUR_GOOGLE_MAPS_API_KEY_HERE');
